# Security Policy

Report vulnerabilities via GitHub private security advisories rather than public issues. This is a static client-side app with no backend, so the surface area is small, but XSS / supply-chain reports are still welcome.
